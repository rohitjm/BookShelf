import React from 'react'
import { Card, CardTitle } from 'material-ui/Card'

const HomePage = () => (
	<Card classname="container">
		<CardTitle title="BookShelf Application" subtitle="This is the bookshelf homepage">
	</Card>	
);

export default HomePage;